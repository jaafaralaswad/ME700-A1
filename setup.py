from setuptools import setup, find_packages

setup(
    name='bisection',           # Name of your package
    version='0.1',              # Version number
    packages=find_packages(),   # Automatically find all packages and subpackages
)
